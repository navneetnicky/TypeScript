const superHeros: string[] = [];

// const heroPower: number[] = [];

// we can also use like this

const heroPower: Array<number> = [];


type User = {
  name: string
  isActive:boolean
}

const allUsers: User[] = [];

const MLModels: number[][] = [  // array in array
  [255,244,222],
  [255,244,222],
]

superHeros.push( 'spiderman' );
heroPower.push( ( 10 ) );

allUsers.push({name:'',isActive:true})